server:
  port: 8087

spring:
  application:
    name: msSucursal
  datasource:
    url: jdbc:mysql://localhost:3306/sucursal_db
    username: root
    password: 
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

springdoc:
  api-docs:
    enabled: true
    version: openapi_3_0
    path: /api/v1/sucursales/v3/api-docs
  swagger-ui:
    enabled: true